class window.RoutinesService

  getRoutines: (next) ->
    # get routines
    next(routines)

  getRoutine: (id, next) ->
    # get routine from from server
    next(routine)

  createRoutine: (data, next) ->
     # post data to server

     next(id)